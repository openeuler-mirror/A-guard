# The token for the code cloud repository, choose one
gitee_token:
gitcode_token:

# repo depend 
requires_repo: 

# The user who builds the environment
build_env_account: 

# Build environment user password
build_env_passwd: 

# Owner of the warehouse
warehouse_owner: 

# Host address of the build environment
build_host: 

# File Server address
files_server: 

# Oecp software package comparison tool comparison result output file
compare_result:

# Working directory
workspace:

# Software package arch
arch:

# prid
pr:

# repo
repo:

# submitter
committer: 

# Mysql database host
db_host:

# Mysql port
port:

# Mysql user name password
user_passwd:

# Pr comment id
commentid:

branch: 

build_env: 

# Trigger build id
build_id:
# ebs website
ebs_server: http://172.16.9.179:30108

# license sbom website
sbom_server: http://172.16.2.173:15551

# code platform gitcode/gitee/github
platform: gitcode

# 构建变体标识（可选），如 64k
variant:

# 变体对应的 EBS 构建宏行（可选），在 build_env_macros 字符串的 macros: 段尾追加
# 每个条目是一个列表，元素为要追加的宏行
variant_macros:
  64k:
    - '"%_with_64kb 1"'
